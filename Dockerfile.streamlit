FROM python:3.10-slim
WORKDIR /app
COPY src/visualization/dashboard/streamlit_app.py .
COPY models/ ./models/
RUN pip install streamlit pandas plotly
EXPOSE 8501
CMD ["streamlit", "run", "streamlit_app.py", "--server.port=8501", "--server.address=0.0.0.0"]
